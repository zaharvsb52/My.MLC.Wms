Ext.define('WebClient.common.controller.IList', {
    extend: 'WebClient.common.Interface',

    /**
     * Вызывает удаление сущностей
     * @param {Array} ids массив EntityId
     */
    destroy: function(ids) {}

});